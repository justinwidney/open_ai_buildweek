/** Broad grouping for a life event — matches the categories in LIFE_SIMULATION_MODEL.md's catalog. */
export type EventCategory = "career" | "housing" | "family" | "financial" | "windfall" | "lifestyle";
