{
  "name": "@control-ai/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Background worker_threads pool that extends simulation runs and persists results.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test \"src/**/*.test.ts\""
  },
  "dependencies": {
    "@control-ai/db": "workspace:*",
    "@control-ai/engine": "workspace:*",
    "@control-ai/shared": "workspace:*",
    "piscina": "^5.3.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.4",
    "drizzle-orm": "^0.45.2"
  }
}
