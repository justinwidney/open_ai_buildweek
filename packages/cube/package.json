{
  "name": "@control-ai/cube",
  "version": "0.1.0",
  "private": true,
  "description": "Cube Core data model (YAML) exposing simulation runs as queryable measures/dimensions. Schema-only scaffold; no server wired up yet.",
  "scripts": {
    "dev": "echo 'To run the Cube server, install cube and point cube.js at a running Postgres instance holding @control-ai/db schema. See README.md.' && exit 0",
    "test": "node --test test/model.test.mjs"
  }
}
