# Platform travel

The traveler provides a small, readable continuity cue when a route platform
is selected. It traverses the same curves used by bridge and stepping-path
geometry, uses elapsed time rather than frame increments, and settles on the
destination. Reduced motion places it instantly.

Primary skills: `.agents/skills/animation-system.md` and
`.agents/skills/threejs.md`.
